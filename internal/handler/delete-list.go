package handler

import (
	// "log"
	// "fmt"
	// "strconv"
	// "tasks"
)

func DeleteList(args []string) {
	// id, err := strconv.Atoi(args[0])
	// if err != nil || id < 0 || id >= len(tasks) {
	// 	fmt.Println("Invalid task ID.")
	// 	return
	// }
	// tasks = append(tasks[:id], tasks[id+1:]...)
	// fmt.Printf("Task %d deleted.\n", id)
}